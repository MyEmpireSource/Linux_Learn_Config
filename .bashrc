# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

# User specific aliases and functions

#2015-04-17 some aliases
alias la='ls -a'
alias rm='rm -i'
alias mkdir_e='mkdir Executable'
